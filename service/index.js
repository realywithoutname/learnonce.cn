let News = require('./news');
let Feed = require('./feed');
let Note = require('./note');
let Tag = require('./tag');
let Demo = require('./demo');

module.exports = {
  News, Feed, Note, Tag, Demo
}
